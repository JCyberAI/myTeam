// TODO: Write code to define and export the Intern class.  HINT: This class should inherit from Employee.
const Employee = require("./Employee");

class Intern extends Employee {

    constructor(school, gpa) {

        const graduated = data.school;
        const gradeaverage = data.gpa;

        super(graduated, gradeaverage);
        this.school;
        this.gpa;
    }

}

const intern = new Intern(school, gpa);
intern.logInfo();